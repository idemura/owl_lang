#! /bin/bash

for owl in `find . -name '*.owl'`; do
    out=`tempfile`
    ./owl $owl > $out
    if [ $? != 0 ]; then
        exit 1
    fi
    diff -ZBu $owl.out $out
    if [ $? != 0 ]; then
        exit 1
    fi
done
echo "ALL TESTS PASSED"
