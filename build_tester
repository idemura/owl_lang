#! /bin/bash

for owl in `find programs -name '*.owl'`; do
    flags=`cat $(dirname "$owl")/FLAGS`
    out=${owl}.out
    err=${owl}.err
    ./owl $flags $owl 1> $out 2> $err
    if [ $? == 0 ]; then
        rm -f $err
    else
        rm -f $out
    fi
done
