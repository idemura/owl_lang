module main;

fn main(argv: String[]) {
    var a = 1;
    var b = 3;
    var c = 2;
    var d = b * b - 4 * a * c;
    if d < 0 {
        println("no solution");
    } elif d == 0 {
        println("single root");
    } else {
        println("different roots");
    }
}
