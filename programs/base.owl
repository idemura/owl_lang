# hello
fn f1() { }
fn f2(z) { ; }

fn f3(x, y: I32) {
    f1();
    f2(x);
    f3(x, y);
    module.f3(mod.x, y);
    arr[x];
    f4()(x)[10];
    (f2)(x);
}

fn f4() {
    g(10, 20) : I32;
    g(10 : String);
    g(50 : I32[]);
}

fn f5(): I32 => Char => Bool {
    50 : I32 => Bool;
    12 : Generic(I32);
    13 : Generic(I32, String, Bool);
    14 : Generic(I32 => Bool, String[], Bool => Char[]);
    15 : I32[][] => I64 => Bool;
}

fn f6() {
    f(~10);
    f(~10 : I32);
    f(- 100);
    f(-100, !(a : I32));
    f(-100, !a : I32);
    f(-100, (!a) : I32);
    +f(10);
    +f(10) : I32;
}

fn f7 {
    2 * 3 / 5;
    2 * 3 / 5  % 9;
    -2 * +4 / -5 % +6;
}
